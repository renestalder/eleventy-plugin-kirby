#!/bin/bash

pwd
ls -all

# cd $PLUGIN_DIR
# if [ -f "package-lock.json" ] ; then npm ci --cache .npm --prefer-offline ; else npm i --cache .npm --prefer-offline ; fi

# cd $PROJECT_DIR
# npm link ${PLUGIN_DIR}
# if [ -f "package-lock.json" ] ; then npm ci --cache .npm --prefer-offline ; else npm i --cache .npm --prefer-offline ; fi